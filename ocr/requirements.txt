# API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Kiri-OCR runtime support
# Install torch from the PyTorch CPU index and kiri-ocr with --no-deps via
# scripts/install_cpu_runtime.sh to avoid pulling unnecessary GPU wheels.
onnxruntime>=1.15.0
huggingface_hub>=0.16.0
safetensors>=0.4.0
pyclipper>=1.3.0
shapely>=2.0.0
PyYAML>=6.0.0
tqdm>=4.65.0

# Image Processing
opencv-python-headless>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# Validation & Settings
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Utilities
python-dotenv>=1.0.0

