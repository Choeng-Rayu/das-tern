


Now want you hyelp me to deploy it into the vps i buy from the digital ocean so this si @beautifulMention throught this terminali have login to that for you and already clone my project repo already but for the .env is not yet add into that (this vps server is use ssh root@167.71.194.68 and password "rayu@dastern@1VPS") 




do not host all the service just this service i mention below 

@beautifulMention
host the docker and contain the database so please set up the docker and host the backend_nestjs, ocr, AI  and database but for the bakong_payment and flutter(frontend is do not host
so please set up the plan for that and host use Nginx and also make sure it work with https you can use certbot so please now do that for me 


now let me tell to to design the domain 
for backend service api.dastern.sit
for ocr service ocr.dastern.site
for ai service ai.dastern.site
for paymnet is host in another vps but you the domain is also like payment.dastern.site(when the backend communication with the bakong it will communicate like through that domain 
so please set up that 