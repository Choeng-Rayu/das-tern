-- supabase/seed.sql
-- Local development seed data. Never put production secrets here.
-- Applied automatically by `supabase db reset`.

-- ── Dev users ────────────────────────────────────────────────────────
-- Passwords are hashed with bcrypt; these are for local testing only.
-- Use `supabase auth admin create-user` or the Studio UI to add users
-- interactively instead.

-- Placeholder: insert seed rows here as tables are created in migrations.
-- Example (uncomment after 01-supabase-data-layer migration lands):
--
-- INSERT INTO profiles (id, role, first_name, timezone)
-- VALUES
--   ('00000000-0000-0000-0000-000000000001', 'PATIENT', 'Test Patient', 'Asia/Phnom_Penh'),
--   ('00000000-0000-0000-0000-000000000002', 'DOCTOR',  'Test Doctor',  'Asia/Phnom_Penh')
-- ON CONFLICT (id) DO NOTHING;
