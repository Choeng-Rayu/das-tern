in doctor_home_tab has design issue which is color is not change when theme color change. update it to align the app theme color. in across of doctor interface make sure it use from only /das-tern/das-tern/das_tern_mcp/lib/l10n/app_localizations_en.dart, das-tern/das_tern_mcp/lib/l10n/app_localizations_km.dart, das-tern/das_tern_mcp/lib/l10n/app_localizations.dart. add new localization of text  of that three file if in doctor interface miss it.
1. implement theme color across doctor interface all screen and patient detail,  and implement localization for fit the project
2. in patient tab of doctor redesign style of das-tern/das_tern_mcp/lib/ui/screens/doctor/patient_list.dart. for patient list should display name of patient, their sickness and status if they taken medication or not. implement only style from that file and keep the logic and everything as the same. make sure the new design align with current implementation
3. in patient detail i want to redesign this by follow the style of das-tern/das_tern_mcp/lib/ui/screens/doctor/patient_detail.dart. keep logic and everything else the same. for this patient detail , in doctor interface, doctor can see list of prescription that patient has been given.
4. in history tab of doctor interface , make sure when prescription is tapped , it will go detail prescription.
5. implement backend and frontend to follow the requirement and verify and test it to make it work.